var fs = require("fs");
var input = fs.readFileSync("stdin", "utf8");
var lines = input.split("\n");

var n = parseInt(lines.shift());

for(var i = 0; i < n; i++){
    bandido = lines.shift();
    console.log("Y");
} 
